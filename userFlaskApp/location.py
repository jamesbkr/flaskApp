from user import User


class Location():
    users = {}
    items = []
    loctions = []
    def __init__(self,name):
        self.name = name

    def add_user(user)

        newUser = User(user.first_name, user.last_name)
        users.append(newUser)
        return true
